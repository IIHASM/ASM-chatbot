import { createApp } from 'vue';
import ChatWidget from './ChatWidget.vue';

const el = document.createElement('div');
el.id = 'chat-widget';
document.body.appendChild(el);
createApp(ChatWidget).mount('#chat-widget');
